<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketharness">

    <!-- fuzzy-launch any installed app -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- brightness control (granted via Settings screen at runtime) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- voice hold-to-talk: on-device SpeechRecognizer, no system dialog -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- haptic confirm on wake / step completion -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- AI planner talks to a user-configured opencode / OpenAI-compatible server -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:usesCleartextTraffic="true">
        <!-- cleartext allowed: opencode serve on the LAN is plain http -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".HarnessService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
